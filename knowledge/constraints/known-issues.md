---
type: Constraint
title: Known issues and rough edges
description: Defects and fragilities identified by reading the code, each with its trigger and the file to look at - items 1, 4, 5, 6, 7, 8, 9, 10, 12, 16, 18, 20, 21, and 22 fixed 2026-09-08, the rest unfixed (items 2 and 13 partially); item 14 is intentional, not a defect.
tags: [constraints, known-issues, defects, technical-debt]
status: stable
generated: { by: claude_code/claude-opus-5, at: 2026-09-05T00:00:00Z }
sources:
  - id: effects
    resource: ../../src/@state/app.effects.ts
    title: AppEffects
  - id: svc
    resource: ../../src/services/spreadsheet/spreadsheet.service.ts
    title: SpreadsheetService
  - id: page
    resource: ../../src/modules/dashboard/dashboard/dashboard-page.container.ts
    title: DashboardPageContainer
  - id: table
    resource: ../../src/shared/components/expenses-table/expenses-table.component.ts
    title: ExpensesTableComponent
---

Everything below was derived by reading the code in this repository and has **not been
reproduced at runtime**. Treat each as a lead, not a verdict.

# Correctness

| # | Issue | Trigger | Where |
|---|---|---|---|
| 1 | ~~**Category reorder rollback never dispatches.**~~ **Fixed 2026-09-08.** The `catchError` builds `AppActions.storeCategories({ categories: this.categoriesBackUp })` but did not pass it to `store.dispatch`; now it does. Bundled into `docs/specs/effect-error-surfacing.md` (D5) because the new failure toast for this effect claims "your categories were put back," which would have been false otherwise. | ~~any failed reorder write~~ (resolved) | `app.effects.ts`, `updateCategoryPosition$`[^effects] |
| 2 | ~~**Delete only sees the newest 100 rows.**~~ **Partially fixed 2026-09-08.** `loadLastExpenses(title, 100)` still caps what the effect can search, so an expense older than the 100 most recent still can't be located and deleted from the sheet — that part is unchanged and would need a real lookup (by row content or a stable id) to fix properly. What changed: the not-found branch used to `return of(undefined)`, silently treating "row not found" as success while the earlier optimistic UI removal stood, so the item vanished from the UI without ever being deleted from the sheet. It now `throw`s (matching the existing `deleteCategory$` not-found convention two effects up in this file), which routes into this same effect's own `catchError` — the one that already restores `deletedExpenseBackup` into the store and dispatches `operationFailed`/the failure toast for genuine errors. So the failure is no longer silent and the optimistic removal is no longer left stranded; the underlying 100-row search limit is unchanged. Found while looking for the cheapest fix in this list — cost was a two-line change (`return of(undefined)` -> `throw`, plus dropping the now-unused `of` import) because it reuses rollback/error-reporting machinery this effect already has for its real error path, rather than building new handling for the not-found case. | deleting an expense older than the 100 most recent | `app.effects.ts`, `deleteExpense$` |
| 3 | **Row deletion is positional.** The array index from a fresh read is used as the sheet row index. A concurrent edit or a manual sort in Google Sheets between read and delete removes the wrong row. | concurrent editing | `deleteExpense$` + `deleteSheetRow` |
| 4 | ~~**gviz row mapping dereferences without guards.**~~ **Fixed 2026-09-08.** `row.c[0].v` and `row.c[2].v` threw on an empty category or amount cell (gviz represents a blank cell as `null` in the `c` array, not `{v: null}`). Now guarded with `row.c[0]?.v ?? ''` and `row.c[2]?.v ?? 0`, matching the `?.v` pattern already used for `comment`/`isInDebt` in the same function. | ~~a blank cell in column A or C~~ (resolved) | `spreadsheet.service.ts`, `loadExpenses`[^svc] |
| 5 | ~~**Serial-number date reversal uses today's timezone offset**~~ **Fixed 2026-09-08.** `new Date().getTimezoneOffset()` used today's DST status for every row. Now the raw value is first converted to its UTC instant, and the offset is read from `new Date(utcTime).getTimezoneOffset()` — that instant's own DST status — before applying it. | ~~reading rows from the other side of a DST change~~ (resolved) | `spreadsheet.service.ts`, `getDateFromSerialNumber`[^svc] |
| 6 | ~~**`LocalStorageService.put` skips falsy values.**~~ **Fixed 2026-09-08.** `if (value)` meant `0`, `''`, `false` were never written — a `categoriesSheetId` of `0` (a valid gid) would not persist. Now checks `value !== undefined && value !== null`. | ~~a spreadsheet whose categories tab is the first sheet~~ (resolved) | `local-storage.service.ts` |
| 7 | ~~**`ngOnChanges` assumes `dataSource` always changes.**~~ **Fixed 2026-09-08.** `changes['dataSource'].currentValue` was read unconditionally, so any change pass without that input threw; now guarded with `changes['dataSource']?.currentValue`. | ~~binding another input alone~~ (resolved) | `expenses-table.component.ts`[^table] |
| 8 | ~~**Non-null assertion on the current sheet.**~~ **Fixed 2026-09-08.** `sheet!.id` in the dashboard constructor threw if no sheet was selected. Now `loadExpenses` is only dispatched when `sheet` is truthy. `isSetupReady` still does not check `selectedSheetId` — a route entry with no sheet selected no longer crashes, but is otherwise unhandled (no expenses load, no redirect). | ~~entering the dashboard with `dataSheets.selectedSheetId === null`~~ (crash resolved; guard gap remains) | `dashboard-page.container.ts`[^page] |
| 9 | ~~**Setup has no error handling.**~~ **Fixed 2026-09-08.** No `catchError` anywhere in `checkSetup`, so any failure hung the spinner forever (the button stayed disabled with no way to retry short of reloading). Now the pipe ends with `catchError -> log(e) -> EMPTY`, resetting `loading` before completing — the same swallow-and-log convention already used everywhere else in this codebase (item 21), not the store-dispatched `operationFailed`/toast convention from item 10, since that would have meant adding `checkSetup` to `report-failure.ts`'s `FailureSource` union and its `FAILURE_MESSAGES` map — which would break `report-failure.spec.ts`'s `[AC8]` test pinning the message set to exactly 7 sources tied to `docs/specs/effect-error-surfacing.md`, a working test not to be touched without permission. Found while looking for the cheapest fix in this list — cost was low because it's a single isolated component method and the swallow-and-log idiom is already established; the toast-integrated version would have cost more (test + spec changes) for the same "no longer hangs" outcome. | permissions, network, bad URL | `setup-page.container.ts` |
| 22 | ~~**gviz row mapping dereferenced the date cell without a guard.**~~ **Fixed 2026-09-08.** The item-4 fix guarded `row.c[0]` and `row.c[2]` (and `row.c[1]`/`row.c[4]` already used `?.v`), but missed `row.c[3].v` (column D, the date) on the very next line — same failure class: gviz represents a blank cell as `null` in the `c` array, not `{v: null}`, so a blank date cell threw inside the `map` callback. Now `row.c[3]?.v` is captured first and only passed to `secureParseDate` when defined; a blank date cell maps to `undefined`, which `Expense.date` already accepts (it's optional). Found while looking for the cheapest fix in this list — cost was low because it reuses the guard idiom and optional field from the item-4 fix. | a blank cell in column D | `spreadsheet.service.ts`, `loadExpenses`[^svc] |

# Fragility and debt

| # | Issue | Note |
|---|---|---|
| 10 | ~~**All errors are silent.**~~ **Fixed 2026-09-08** for the 7 remote-calling effects — see `docs/specs/effect-error-surfacing.md`. Each now dispatches `operationFailed({ source, message })`, sets `AppState.lastError`, and `showFailureToast$` opens a `MatSnackBar`. `setup-page.container.ts` got a narrower fix the same day (item 9: swallow-and-log, no toast). **Still true** for the 4 localStorage-only persist effects (item 20). | localStorage effects |
| 11 | **The gviz endpoint is a charting API used as a query API.** Unversioned relative to Sheets; its envelope has changed before (commit `cdc85e6`). | [gviz](/interfaces/gviz-query.md) |
| 12 | ~~**The on-page logger is always on.**~~ **Fixed 2026-09-08.** `loggerType` was hard-coded to `'window'`, so the overlay's floating toggle button was planted on every page for every production user. Now `loggerType` is read from `?logger=` (mirroring the existing pattern in `app.config.ts`), and the `ExpLogger` overlay is only constructed when it equals `'window'`. The naive version of this fix (just gating the whole block, per the pre-existing commented-out line) is a trap: `log()` is called unconditionally in every effect's `catchError` throughout the app, so leaving `window.log` undefined by default turns silent-swallow error handling into uncaught `ReferenceError`s — caught by 3 failing tests in `report-failure.spec.ts` (which deliberately exercises the real `src/logger` side-effect import rather than stubbing `log`, see its own comment) before this reached the doc. The actual fix splits the two concerns: `window.log = console.log.bind(console)` is installed unconditionally first, and the on-page overlay upgrades it only when opted into via the URL param. Found while looking for the cheapest fix in this list; turned out to need one extra line (the unconditional console fallback) once the trap surfaced, but no test or production-code interface changed otherwise. | `src/logger.ts` |
| 13 | ~~**Three test suites are `describe.skip`**~~ **Partially fixed 2026-09-08.** `LocalStorageService` and `ExpDialogComponent` were unskipped and now pass: the former only needed `LocalStorageService` added to the test's `TestBed` providers (it isn't `providedIn: 'root'`, so `TestBed.inject` had nothing to resolve against under `configureTestingModule({})`); the latter had no actual problem — it passed as soon as `.skip` was removed. **Still true** for `AppComponent`: its suite needs `AbstractSecurityService`, `NetworkStatusService`, `SpreadsheetService`, and `SwUpdate` mocked (none are provided in its `TestBed` module, so `TestBed.createComponent` would throw before the assertions even run), and separately its "should render title" test asserts `.content span` contains `'exp-spsh app is running!'` — CLI-scaffold boilerplate `app.component.html` no longer has; the `.content` div now holds only a `<router-outlet>`. Left skipped rather than guessed at, since writing correct mocks for four services and deciding what the title assertion should check instead are both judgment calls, not one-line fixes. Found while looking for the cheapest fix in this list — the two trivial suites cost a one-line provider addition and a one-line `.skip` removal respectively; `AppComponent` was priced out as the expensive remainder. | [testing](/operations/testing.md) |
| 14 | **CI runs no tests, intentionally.** A failing suite still deploys. A candidate fix (add `npm test` before `npm run build` in `webpack.yml`) was implemented and verified 2026-09-08, then reverted at the user's request: running the suite on every push/PR costs CI minutes the project has decided not to spend, so this is a deliberate economy trade-off, not an oversight to close. Revisit only if that trade-off changes. | [CI](/operations/ci-and-deployment.md) |
| 15 | **`ExpDialogComponent` is unreferenced** except through `UIKitModule` imports — the destructive delete gestures have no confirmation. | `src/shared/components/dialog/` |
| 16 | ~~**Dead configuration.**~~ **Fixed 2026-09-08.** `environment.*.ts` `SCOPES`/discovery docs, the `BACK` UI constant, and `SpreadsheetService.append` (`@deprecated`, uncalled) were removed after confirming no references anywhere in `src/`. | [configuration](/operations/configuration-and-secrets.md) |
| 18 | ~~**The `.claude/settings copy.json` hook points at a missing script**~~ **Fixed.** That file was deleted in commit `7e2796f` ("add agents scaffold"); `.claude/settings.json` is the only settings file now, and its one hook (`PreToolUse` → `deny-git-push.mjs`) points at a script that exists. Found while looking for the cheapest fix in this list — cost was zero code change, just confirming the stale file/script were already gone and updating this doc. | [working agreements](/constraints/working-agreements.md) |
| 19 | **Duplicate commit history around `isInDebt`** (`d787877` and `00eda29` share a message), and a revert pair (`98f2b23` / `3075d41`) — the debt feature settled through trial and error. | git history |
| 20 | ~~**The 4 localStorage-only persist effects have no failure path.**~~ **Fixed 2026-09-08.** `saveSpreadsheetId$`, `saveSheetId$`, `saveCategoriesSheetId$`, `saveCategories$` had no `catchError` at all — a throwing `LocalStorageService.put` (quota exceeded, Safari private-browsing restrictions) was fully uncaught. Each now ends with the same swallow-and-log `catchError((e) => { log(e); return EMPTY; })` idiom used elsewhere in this file and in item 9 — not the toast-integrated remedy from item 10, since these are a different failure class (synchronous, non-network, non-retryable) and none of the 7 `FAILURE_MESSAGES` strings fit; `storage-write-failures` remains unwritten as the follow-up spec for a dedicated remedy. Found while looking for the cheapest fix in this list — cost was low because it's the same one-line idiom already proven safe in three other places, applied to four effects with no dedicated spec test to disturb (`app.effects.spec.ts` only mentions them in a comment). | ~~quota exceeded / private browsing storage restrictions~~ (resolved) | `app.effects.ts`, the 4 `{ dispatch: false }` persist effects |
| 21 | ~~**Every remote effect goes permanently unresponsive after its first failure of the session.**~~ **Fixed 2026-09-08 for all 7 remote effects.** All 7 had their `catchError` on the *outer* pipe, returning `EMPTY` on failure — which *completes* that effect's stream; NgRx's default effects error handler resubscribes on an **error** notification, not on a **completion**, so one failure permanently silenced the effect for the rest of the session. Fixed by moving each `catchError` (`reportFailure(...)`, or for `updateCategoryPosition$` and `deleteExpense$` an inline rollback handler) inside the `exhaustMap` projection, chained onto the async call, with any operators that used to follow on the outer pipe folded in alongside it — a failure now completes only that inner observable, which `exhaustMap`/`switchMap` treats as "ready for the next value," leaving the outer `actions$` subscription alive. Two effects needed more than a mechanical move: `deleteCategory$`'s synchronous `throw` on a not-found category (which fires *before* any observable exists to `.pipe()` onto) became a `throwError(() => ...)` branch feeding the same inner pipe, matching the pattern item 2 already established for `deleteExpense$`'s own not-found branch. `deleteExpense$` chains two async calls (search-then-delete) around `deletedExpenseBackup` rollback state read across both — the two top-level `exhaustMap`s were collapsed into one, with the second call continued via an inner `switchMap` inside the first's projection, so the single relocated `catchError` still sees both failure modes (search failing, and delete failing after a not-found `throw`) with the same rollback logic as before, unchanged. No test pinned any of the old operator orders (checked `app.effects.spec.ts` and `report-failure.spec.ts`), so every effect was a same-behavior reshuffle for the success path plus the actual resubscription fix for the failure path. Follow-up spec named but not written: `effect-resubscription` — no longer needed as a distinct task, closed out here. | any second failure of the same effect, same session (resolved) | `app.effects.ts`, all 7 remote effects |

[^effects]: AppEffects
[^svc]: SpreadsheetService
[^table]: ExpensesTableComponent
[^page]: DashboardPageContainer
